import { View, Text } from 'react-native'
import React from 'react'

export default function RechargeAndPaymentsScreen() {
    return (
        <ScrollView contentContainerStyle={{ width: '100%', height: '100%', backgroundColor: '#fff', justifyContent: 'center', alignItems: 'center' }}>
            <Text>RechargeAndPaymentsScreen</Text>
        </ScrollView>
    )
}
